return {
  settings = {
    tailwindCSS = {
      experimental = {
        classRegex = {
          [[tw`([^`]*)]],
          [[tw\\..+`([^`]*)]],
          [[tw\\(.*?\\).*?`([^`]*)]],
          [[css`([^`]*)]],
          [[css\\..+`([^`]*)]],
          [[css\\(.*?\\).*?`([^`]*)]],
          [[tw`([^`]*)]],
          [[tw=\"([^\"]*)]],
          [[tw={\"([^\"}]*)]],
          [[tw\\.\\w+`([^`]*)]],
          [[tw\\(.*?\\)`([^`]*)]],
        },
      },
    },
  },
}
